// tsconfig.node.json (Correction)
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  // --- REMOVE THIS LINE ---
  // "include": ["vite.config.ts"] 
  
  // Vercel may require an 'include' array, but if you're not using it,
  // it should be empty or point to a file that exists. 
  // For safety, remove the reference or set it to an empty array:
  "include": []
}